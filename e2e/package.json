{
  "name": "waltid-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "test:video:combined": "playwright test && ./merge-playwright-videos.sh",
    "test:cross:record": "playwright test tests/cross-ui-*.spec.ts --project=chromium --workers=1 && ./merge-playwright-videos.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
